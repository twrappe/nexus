# Stimulus profiles for nexus_stimulus_node.
# Each profile defines the signal distribution parameters for a named event class.
# TODO: add per-class profile YAML files (e.g. nominal.yaml, anomaly.yaml, critical.yaml)
